# Project TODOs

* lib\services\ai_service.dart:272 - // NOTE: Pollinations' anonymous quota is granted based on the request's
* lib\screens\ai_assistant_screen.dart:24 - /// NOTE: this is deliberately a PLAIN widget mounted inside the app's own
* lib\screens\ai_assistant_screen.dart:1326 - // NOTE: busy stays TRUE through the whole fallback chain — the
* lib\services\notification_service.dart:37 - // wall-clock time. NOTE: on many MIUI/Redmi builds
* lib\services\reply_cleaner.dart:24 - // header of any level) followed by `:`/`—`/`-`. NOTE: constructor
* lib\services\reply_cleaner.dart:104 - // NOTE: use Cyrillic-aware classes here — Dart's `\w` matches only
* lib\services\storage_service.dart:260 - /// NOTE on cross-device media: backups only carry media *paths*, not

Total: 7 items
