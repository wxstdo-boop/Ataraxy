<?xml version="1.0" encoding="utf-8"?>
<!-- Launch window: warm tint only — no second logo splash. The Flutter
     splash screen (gradient + logo) is the ONE splash; this is just the
     window background so the native phase isn't white. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launchBg" />
</layer-list>
